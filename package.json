{
  "name": "nouns-data-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile prodFile"
  },
  "dependencies": {
    "hono": "^4.6.2",
    "mongodb": "^6.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
